<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.homi.model.dao.mapper.CommunityMapper">

    <select id="getCommunityRoomCount" resultType="java.lang.Integer">
        SELECT COUNT(*)
        FROM room
        WHERE house_id IN (SELECT id FROM house WHERE community_id = #{communityId} and lease_mode = #{leaseMode} and company_id = #{companyId})
    </select>
</mapper>
