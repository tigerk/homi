<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.homi.model.mapper.TenantMapper">

    <select id="pageTenantList" resultType="com.homi.domain.vo.tenant.TenantListVO">
        select
        t.tenant_id,
        t.tenant_name,
        t.tenant_phone,
        t.tenant_email,
        t.tenant_status,
        t.create_time,
        t.update_time
        from tenant t
        <where>
            <if test="tenantName != null">
                and t.tenant_name like concat('%', #{tenantName}, '%')
            </if>
            <if test="tenantPhone != null">
                and t.tenant_phone like concat('%', #{tenantPhone}, '%')
            </if>
        </where>
        order by t.create_time desc
    </select>
</mapper>
